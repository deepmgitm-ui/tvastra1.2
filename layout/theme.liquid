<!doctype html>
<html class="no-js" lang="{{ request.locale.iso_code }}"> 
  <head>
    <meta name="facebook-domain-verification" content="d0cizjy2j428b7lgod9qds2a44s777" />
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="theme-color" content="{% if settings.mobile_browser_color != blank %}{{ settings.mobile_browser_color }}{% endif %}">
    <meta name="format-detection" content="telephone=no">
    <meta name="facebook-domain-verification" content="3vmsz2dcovc10iuzz4vly7hjr1mo7u" />
    <link rel="canonical" href="{{ canonical_url }}">
    <link rel="dns-prefetch" href="https://cdn.shopify.com" crossorigin>
    <link rel="dns-prefetch" href="https://fonts.shopifycdn.com" crossorigin>
    <link rel="dns-prefetch" href="https://fonts.google.com" crossorigin>
    <link rel="dns-prefetch" href="https://review-images.judge.me" crossorigin>
    <link rel="preconnect" href="https://cdn.shopify.com" crossorigin>
    <link rel="preconnect" href="https://review-images.judge.me" crossorigin>
    {%- render 'seo-title' -%}
    {%- render 'meta-tags' -%} 
    <link rel="preload" as="style" href="{{ 'base.min.css' | asset_url }}">
    <script src="{{ 'global.js' | asset_url }}" defer="defer"></script>

    {{ content_for_header }}
    {%- unless settings.use_body_google_font == true or settings.alt_google_fonts == true -%}
      <link rel="preconnect" href="https://fonts.shopifycdn.com" crossorigin>
    {%- endunless -%}
    {%- if settings.use_body_google_font == true or settings.alt_google_fonts == true -%}
      <link rel="preconnect" href="https://fonts.googleapis.com">
      <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    {%- endif -%}
    {%- if settings.body_google_fonts == settings.alt_google_fonts -%}
      <link href="https://fonts.googleapis.com/css?family={{ settings.body_google_fonts }}:100,200,300,400,500,600,700,800,900&display=swap" rel="stylesheet">
    {%- else -%}
      {%- if settings.use_body_google_font == true -%} 
        <link href="https://fonts.googleapis.com/css?family={{ settings.body_google_fonts }}:100,200,300,400,500,600,700,800,900&display=swap" rel="stylesheet">
      {%- endif -%}
      {%- if settings.use_alt_google_font == true -%}
        <link href="https://fonts.googleapis.com/css?family={{ settings.alt_google_fonts }}:100,200,300,400,500,600,700,800,900&display=swap" rel="stylesheet">
      {%- endif -%}
    {%- endif -%}
    {% if settings.enable_loading_img and settings.image_loading != blank %}
      {%- style -%}
        [data-image-loading]:before {
          background-image: url({{ settings.image_loading | image_url: width: 100, height: 100 }});
          background-repeat: no-repeat;
          background-position: center;
          background-size: 80px;
        }
      {%- endstyle -%}
    {% endif %}
    {{ 'critical.min.css' | asset_url | stylesheet_tag }}
    {%- if request.path contains 'account' -%}
      {{ 'customer.min.css' | asset_url | stylesheet_tag }}
    {%- endif -%}
    {%- if template.name == 'product' -%}
      {{ 'product-photoswipe.min.css' | asset_url | stylesheet_tag }}
      {{ 'product-plyr.css' | asset_url | stylesheet_tag }}
    {%- endif -%}
    {{ 'font-awesome-icon.min.css' | asset_url | stylesheet_tag }}
    {%- if settings.bootstrap_icon == true -%}
      {{ 'bootstrap-icons.min.css' | asset_url | stylesheet_tag }}
    {%- endif -%}
    {%- if settings.feather_icon == true -%}
      {{ 'feather-icon.min.css' | asset_url | stylesheet_tag }}
    {%- endif -%}
    {{ 'theme-vendor.min.css' | asset_url | stylesheet_tag }}
    {%- render 'css-variables' -%}
    {%- if settings.enable_promotion == true -%}
      {%- if settings.promotion_banner_style == 'style-1' -%}
        {{ 'promotion-style-1.min.css' | asset_url | stylesheet_tag }}
      {%- else -%}
        {{ 'promotion-style-2.min.css' | asset_url | stylesheet_tag }}
      {%- endif -%}
    {%- endif -%}
    {{ 'base.min.css' | asset_url | stylesheet_tag }}
    {%- if settings.header_layout == 'style-2' -%}
      {{ 'header-style-2.min.css' | asset_url | stylesheet_tag }}
    {%- endif -%}
    {{ 'responsive.min.css' | asset_url | stylesheet_tag }}
    {{ 'custom.css' | asset_url | stylesheet_tag }}
    <link rel="stylesheet" href="{{ 'product-stock-state.css' | asset_url | split: '?' | first }}?v=1">
    {{ 'review-stars-fix.css' | asset_url | stylesheet_tag }}
    {{ 'review-widget-v2.css' | asset_url | stylesheet_tag }}
    <script>document.documentElement.className = document.documentElement.className.replace('no-js', 'js');    
    </script>
    <style>
  @font-face {
    font-family: 'FuturaCustom'; /* Ye hamara secret naam hai */
    src: url('https://cdn.shopify.com/s/files/1/0641/9395/3905/files/Futura_PT.woff2?v=1781949181') format('woff2');
    font-weight: normal;
    font-style: normal;
    font-display: swap;
  }
</style>
  </head>
  {%- liquid
    assign purchase_btn = false
    if settings.enable_login_price == true and customer and settings.enable_catalog_mode == false
      assign purchase_btn = true
    elsif  settings.enable_login_price == false and settings.enable_catalog_mode == false 
      assign purchase_btn = true
    elsif settings.enable_catalog_mode == true and customer and settings.enable_login_price == false
      assign purchase_btn = true
    elsif settings.enable_catalog_mode == true and settings.enable_login_price == false
      assign purchase_btn = false
    elsif settings.enable_login_price == true and customer and settings.enable_catalog_mode == true 
      assign purchase_btn = true
    elsif settings.enable_login_price == true and settings.enable_catalog_mode == true
      assign purchase_btn = false
    endif
  -%}
  <body class="{% if purchase_btn == false %}purchase-off {% endif %}{% unless settings.show_variant_option %}product-box-no-variant {% endunless %}"{% if settings.enable_ajex_data == true  %}data-ajex="{{ settings.enable_ajex_data }}"{% endif %} {%- if settings.body_bg_image != blank -%} style="background-image: url('{{ settings.body_bg_image | image_url }}');background-repeat: {%- if settings.repeat_bg_image == true -%}repeat;{%- else -%}no-repeat;{%- endif -%}"{%- endif -%}>
    {%- if settings.enable_box_layout == true -%}
      <div class="box-layout">
    {%- endif -%}
      <a class="skip-to-content-link button visually-hidden" href="#MainContent">
        {{ "accessibility.skip_to_text" | t }}
      </a>
      {%- section 'mini-header' -%}
        {%- render 'header' -%}
      <main id="MainContent" class="content-for-layout focus-none {% if template == 'product' %}product-page{% endif %}" role="main" tabindex="-1">
        {{ content_for_layout }}
      </main>
      {%- liquid
        if settings.footer_style == "style-1"
          section 'footer'
        elsif settings.footer_style == "style-2" 
          section 'footer-with-USP'
        else
          section 'footer-3'
        endif
      -%}
      {%- section 'mobile-sticky-menu' -%}
      {%- section 'static-advertising' -%}
      <ul hidden>
        <li id="a11y-refresh-page-message">{{ 'accessibility.refresh_page' | t }}</li>
      </ul>      
      {%- if settings.social_visible == 'index' -%}
        {%- if template == 'index' -%}
          <div class="social-wrapper"> 
            {%- render 'social-follow', style_class: settings.social_type, social_mode: settings.social_style -%}
          </div>
        {%- endif -%}
      {%- elsif settings.social_visible == 'all-page' -%}
        <div class="social-wrapper"> 
          {%- render 'social-follow', style_class: settings.social_type, social_mode: settings.social_style -%}
        </div>
      {%- endif -%}
      {%- liquid
        assign should_newsletter_enable = settings.enable_popup_newsletter
        if should_newsletter_enable == true and settings.enable_newsletter_design_mode != true and request.design_mode == true
          assign should_newsletter_enable = false
        endif
        if should_newsletter_enable == true
          render 'newsletter-model'
        endif

        assign should_promotion_enable = settings.enable_promotion
        if should_promotion_enable == true and settings.enable_promotion_design_mode != true and request.design_mode == true
          assign should_promotion_enable = false
        endif
        if should_promotion_enable == true
          render 'promotion-model'
        endif
        
        assign should_enable = settings.enable_cookie_consent
        if should_enable == true and settings.enable_design_mode != true and request.design_mode == true
          assign should_enable = false
        endif
        if should_enable == true
          render 'cookie-consent'
        endif
      -%}
        <script type="text/javascript">   
          window.Hongo = {
            shop: '{{ shop.permanent_domain }}',
            {% comment %} design_mode: {{ request.design_mode | json }}, {% endcomment %}
            {%- if request.design_mode == true -%}
              purchase_code: '{{ settings.purchase_code }}',
              purchase_action: '{{ settings.purchase_action }}',
              shop_info: {
                id: {{ shop.id | json }},
                domain: {{ shop.domain | json }},
                name: {{ shop.name | json }},
                shop_owner: {{ shop.shop_owner | json }},
                email: {{ shop.email | json }},
                phone: {{ shop.phone | json }},
                country_code: {{ shop.country_code | json }},
                country_name: {{ shop.country_name | json }},
                plan_name: {{ shop.plan_name | json }}
              },
            {%- endif -%}
          }
        </script>
      <script src="{{ 'jquery.min.js' | asset_url }}"></script>
      <script src="{{ 'variants.js' | asset_url | split: '?' | first }}?v=smooth-variant-v3" defer="defer"></script>
      <script src="{{ 'theme-vendors.min.js' | asset_url }}" defer="defer"></script>
      {%- if settings.enable_popup_newsletter == true or settings.enable_promotion == true or settings.enable_cookie_consent == true -%}
        <script src="{{ 'cookie.js' | asset_url }}" defer="defer"></script>
      {%- endif -%}
      {%- if template.name == 'product' -%}
        <script src="{{ 'product-photoswipe-lib.min.js' | asset_url }}" defer="defer"></script>
        <script src="{{ 'plyr.min.js' | asset_url }}" defer="defer"></script>
      {%- endif -%}
      <script src="{{ 'lazy-images.js' | asset_url }}" async></script>      
      {%- if should_newsletter_enable == true -%}
        <script src="{{ 'newsletter.js' | asset_url }}" defer="defer"></script>
      {%- endif -%}
      <script src="{{ 'main.min.js' | asset_url }}" defer="defer"></script>
      {%- if template.name == 'collection' -%}
        <script src="{{ 'collection-load-more.js' | asset_url }}" defer="defer"></script>
      {%- endif -%}
      <script src="{{ 'cart-notification.js' | asset_url | split: '?' | first }}?v=cart-premium-v8" defer="defer"></script>
      {%- if settings.show_compare == true -%}
  <script src="{{ 'compare.js' | asset_url }}" defer="defer"></script>
{%- endif -%}
      {%- if template.name == 'product' -%}
  <script src="{{ 'product-detail.js' | asset_url }}" defer="defer"></script>
{%- endif -%}
      {%- if template.name == 'product' -%}
        <script src="{{ 'product-inline-media.js' | asset_url | split: '?' | first }}?v=gallery-controller-v2" defer="defer"></script>
        <script src="{{ 'product-live-viewers.js' | asset_url | split: '?' | first }}?v=live-viewers-v2" defer="defer"></script>
      {%- endif -%}
      {%- if should_promotion_enable == true -%}
        <script src="{{ 'promotion.js' | asset_url }}" defer="defer"></script>
      {%- endif -%} 
      {%- liquid
        render 'custom-cursor'
        if settings.scroll_to_top
          render 'scroll-progress'
        endif
        render 'internal-style'
        render 'internal-script'
      -%}
    {%- if settings.enable_box_layout == true -%}
      </div>
    {%- endif -%}
    {%- render 'form-info' -%}
    {%- if settings.show_promotion_btn == true -%}
      {%- render 'promotion-button' -%}
    {%- endif -%}
    {%- render 'loader',main-loader:true -%}
    {% unless request.path contains 'return_prime' %}
    <script src="{{ 'jsloader.js' | asset_url }}" defer="defer"></script>
    {% endunless %}
    {%- if template.name == 'index' -%}
      {%- render 'whatsapp-floating' -%}
    {%- endif -%}

    {%- if template.name == 'product' or template.name == 'cart' or template.name == 'collection' -%}
  {% render 'razorpay-magic-checkout' %}
{%- endif -%}


    <style>
  /* =========================================
     1. WISHLIST ICON & HEADER FIX (100% Safe)
  ========================================= */
  a[href*="wishlist"] {
    display: inline-flex !important;
    align-items: center !important;
    justify-content: center !important;
  }
  a[href*="wishlist"] svg, 
  a[href*="wishlist"] i {
    width: 24px !important; 
    height: 24px !important;
    max-width: 24px !important;
    max-height: 24px !important;
    margin: 0 !important;
    transform: translateY(-2px) !important; 
  }
  a[href*="wishlist"] svg path {
    stroke-width: 1.5 !important; 
  }
  sticky-header, .header-wrapper, .header-section, header, #shopify-section-header, .shopify-section-header {
      z-index: 99999 !important;
      position: relative;
  }

  /* =========================================
     2. THUMBNAILS OVERLAP & TOP SPACE FIX
  ========================================= */
  .product-gallery.slider-horizontal { 
      position: relative !important; 
      padding-top: 0 !important; 
      padding-bottom: 0 !important; 
  }
  
  /* Container se upar ka faltu space hatane ke liye */
  .product-gallery.slider-horizontal .mobille-media-slider { 
      position: relative !important; 
      display: block !important; 
      margin-top: 0 !important;
      padding-top: 0 !important;
      top: 0 !important;
  }
  
  .product-gallery.slider-horizontal .product-main-slider { 
      width: 100% !important; 
      max-width: 100% !important; 
      margin-top: 0 !important;
      margin-bottom: 0 !important; 
  }

  /* YE WAPAS NORMAL KIYA: Taaki image wapas dikhne lage */
  .product-gallery.slider-horizontal .product-main-slider .swiper-slide {
      display: block !important; 
      height: 100% !important; 
  }
  
  /* IMAGE KO TOP PAR CHIPKANE KA SAFE TAREEKA */
  .product-gallery.slider-horizontal .product-main-slider img {
      object-position: top center !important; /* Ye image ko hamesha upar rakhega */
      margin-top: 0 !important;
      padding-top: 0 !important;
  }
  
  /* Chhote boxes image ke andar overlap karenge */
  .product-gallery.slider-horizontal .product-thumb-wrap {
      position: absolute !important;
      bottom: 20px !important; 
      left: 50% !important;
      transform: translateX(-50%) !important;
      width: 100% !important;
      max-width: 500px !important; 
      z-index: 99 !important;
      margin: 0 !important;
      padding: 0 10px !important;
      pointer-events: auto !important; 
  }

  .product-gallery.slider-horizontal .product-image-thumb .swiper-wrapper {
      display: flex !important;
      flex-direction: row !important;
      align-items: center !important;
      justify-content: center !important;
  }

  .product-gallery.slider-horizontal .product-image-thumb .swiper-slide {
      width: 60px !important;
      height: auto !important;
      opacity: 0.6 !important;
      transition: all 0.3s ease !important;
      border-radius: 6px !important;
      cursor: pointer !important;
      border: 2px solid transparent !important;
      box-shadow: 0px 4px 6px rgba(0,0,0,0.3) !important;
  }
  
  .product-gallery.slider-horizontal .product-image-thumb .swiper-slide:hover,
  .product-gallery.slider-horizontal .product-image-thumb .swiper-slide-thumb-active,
  .product-gallery.slider-horizontal .product-image-thumb .swiper-slide.swiper-slide-active {
      opacity: 1 !important;
      border: 2px solid #fff !important; 
      transform: scale(1.05); 
  }
  
  .product-gallery.slider-horizontal .product-image-thumb .swiper-slide img { 
      border-radius: 4px !important; 
      display: block !important; 
  }

  @media (max-width: 767px) {
    .main-product-page .product-gallery.slider-horizontal .mobille-media-slider,
    .main-product-page .product-gallery.slider-vertical .mobille-media-slider {
      position: relative !important;
      display: block !important;
    }

    .main-product-page .product-gallery .product-main-slider {
      position: relative !important;
      order: 1 !important;
      width: 100% !important;
      max-width: 100% !important;
    }

    .main-product-page .product-gallery .product-image-main,
    .main-product-page .product-gallery .product-image-main .swiper-wrapper,
    .main-product-page .product-gallery .product-image-main .swiper-slide,
    .main-product-page .product-gallery .product-image-main .product-media {
      height: min(52svh, 460px) !important;
    }

    .main-product-page .product-gallery .product-image-main .product-media img {
      width: 100% !important;
      height: 100% !important;
      object-fit: contain !important;
    }

    .main-product-page .product-gallery .product-thumb-wrap {
      position: absolute !important;
      right: 12px !important;
      bottom: 12px !important;
      left: 12px !important;
      z-index: 5 !important;
      width: auto !important;
      max-width: none !important;
      margin: 0 !important;
      padding: 0 !important;
      transform: none !important;
      overflow: hidden !important;
    }

    .main-product-page .product-gallery .product-thumb-wrap .product-image-thumb {
      width: 100% !important;
      max-height: none !important;
      margin-top: 0 !important;
      overflow: visible !important;
      padding: 0 2px !important;
      touch-action: pan-x !important;
    }

    .main-product-page .product-gallery .product-thumb-wrap .product-image-thumb .swiper-wrapper {
      align-items: stretch !important;
      justify-content: flex-start !important;
    }

    .main-product-page .product-gallery .product-thumb-wrap .product-image-thumb .swiper-slide {
      width: clamp(52px, 15vw, 64px) !important;
      height: auto !important;
      flex-shrink: 0 !important;
      opacity: 0.78 !important;
      border: 1px solid rgba(255, 255, 255, 0.7) !important;
      border-radius: 5px !important;
      background: rgba(255, 255, 255, 0.25) !important;
    }

    .main-product-page .product-gallery .product-thumb-wrap .product-image-thumb .swiper-slide-thumb-active {
      opacity: 1 !important;
      border-color: #fff !important;
    }
  }

  /* =========================================
     3. GLOBAL SWATCH BUTTONS 
  ========================================= */
  .color-swatch-box-v2 { margin: 20px 0 !important; padding: 5px 0 !important; }
  .swatch-label-v2 { display: block !important; margin-bottom: 8px !important; font-weight: 700 !important; font-size: 14px !important; color: #000 !important; }
  .swatch-flex-v2 { display: flex !important; gap: 12px !important; align-items: center !important; flex-wrap: wrap !important; }
  .gol-button-final { width: 32px !important; height: 32px !important; border-radius: 50% !important; display: block !important; border: 2px solid #ccc !important; cursor: pointer !important; transition: all 0.2s ease !important; box-sizing: border-box !important; box-shadow: 0px 2px 4px rgba(0,0,0,0.1) !important; }
  .gol-button-final:hover { transform: scale(1.15) !important; border-color: #000 !important; box-shadow: 0px 3px 6px rgba(0,0,0,0.2) !important; }
</style>

     <script src="{{ 'scroll-logo.js' | asset_url | split: '?' | first }}?v=review-stars-v1" defer></script>
     <script src="{{ 'review-stars.js' | asset_url }}" defer></script>
     {%- if template.name == 'index' -%}
       <script src="{{ 'judgeme-review-cards.js' | asset_url }}" defer></script>
     {%- endif -%}

    
    {% unless customer %}
<script>
(function () {
  if (window.__tvastraSpurCleanLoaded) return;
  window.__tvastraSpurCleanLoaded = true;

  var FIRST_DELAY = 3000;
  var REPEAT_DELAY = 60000;
  var SUBMITTED_KEY = 'tvastra_spur_submitted_clean_v2';

  if (localStorage.getItem(SUBMITTED_KEY) === 'yes') return;

  function textOf(el) {
    return (
      (el.innerText || '') + ' ' +
      (el.getAttribute && (el.getAttribute('aria-label') || '')) + ' ' +
      (el.getAttribute && (el.getAttribute('title') || '')) + ' ' +
      (el.id || '') + ' ' +
      ((el.getAttribute && el.getAttribute('class')) || '')
    ).toLowerCase();
  }

  function isSpurElement(el) {
    while (el && el !== document.documentElement) {
      var id = (el.id || '').toLowerCase();
      var cls = (el.getAttribute && (el.getAttribute('class') || '').toLowerCase()) || '';
      if (id.indexOf('spur') > -1 || cls.indexOf('spur') > -1) return true;
      el = el.parentElement;
    }
    return false;
  }

  function isVisible(el) {
    if (!el) return false;
    var style = window.getComputedStyle(el);
    var rect = el.getBoundingClientRect();
    return style.display !== 'none' &&
      style.visibility !== 'hidden' &&
      rect.width > 20 &&
      rect.height > 20;
  }

  function clearSpurDismissMemory() {
    try {
      Object.keys(localStorage).forEach(function (key) {
        var k = key.toLowerCase();

        if (
          k.indexOf('spur') > -1 &&
          (
            k.indexOf('dismiss') > -1 ||
            k.indexOf('close') > -1 ||
            k.indexOf('shown') > -1 ||
            k.indexOf('seen') > -1 ||
            k.indexOf('display') > -1 ||
            k.indexOf('frequency') > -1 ||
            k.indexOf('popup') > -1
          ) &&
          k.indexOf('submit') === -1 &&
          k.indexOf('success') === -1 &&
          k.indexOf('lead') === -1 &&
          k.indexOf('customer') === -1
        ) {
          localStorage.removeItem(key);
        }
      });
    } catch (e) {}

    try {
      Object.keys(sessionStorage).forEach(function (key) {
        var k = key.toLowerCase();

        if (
          k.indexOf('spur') > -1 &&
          (
            k.indexOf('dismiss') > -1 ||
            k.indexOf('close') > -1 ||
            k.indexOf('shown') > -1 ||
            k.indexOf('seen') > -1 ||
            k.indexOf('display') > -1 ||
            k.indexOf('frequency') > -1 ||
            k.indexOf('popup') > -1
          ) &&
          k.indexOf('submit') === -1 &&
          k.indexOf('success') === -1 &&
          k.indexOf('lead') === -1 &&
          k.indexOf('customer') === -1
        ) {
          sessionStorage.removeItem(key);
        }
      });
    } catch (e) {}

    try {
      document.cookie.split(';').forEach(function (cookie) {
        var name = cookie.split('=')[0].trim();
        var k = name.toLowerCase();

        if (
          k.indexOf('spur') > -1 &&
          (
            k.indexOf('dismiss') > -1 ||
            k.indexOf('close') > -1 ||
            k.indexOf('shown') > -1 ||
            k.indexOf('seen') > -1 ||
            k.indexOf('display') > -1 ||
            k.indexOf('frequency') > -1 ||
            k.indexOf('popup') > -1
          ) &&
          k.indexOf('submit') === -1 &&
          k.indexOf('success') === -1
        ) {
          document.cookie = name + '=; Max-Age=0; path=/';
        }
      });
    } catch (e) {}
  }

  function findSpurForms() {
    return Array.prototype.slice.call(document.querySelectorAll('form')).filter(function (form) {
      return isSpurElement(form) && isVisible(form);
    });
  }

  function hideSpurCloseButton() {
    document.querySelectorAll('button,a,[role="button"]').forEach(function (btn) {
      if (!isSpurElement(btn)) return;

      var t = textOf(btn).trim();
      var isClose =
        t.indexOf('close') > -1 ||
        t.indexOf('dismiss') > -1 ||
        t === 'x' ||
        t === '×';

      if (!isClose) return;

      btn.style.setProperty('display', 'none', 'important');
      btn.style.setProperty('visibility', 'hidden', 'important');
      btn.style.setProperty('opacity', '0', 'important');
      btn.style.setProperty('pointer-events', 'none', 'important');
      btn.setAttribute('aria-hidden', 'true');
      btn.setAttribute('tabindex', '-1');
    });
  }

  function compactSpurFormOnly() {
    findSpurForms().forEach(function (form) {
      form.style.setProperty('padding', '8px 10px 10px', 'important');
      form.style.setProperty('gap', '6px', 'important');

      form.querySelectorAll('h1,h2,h3').forEach(function (el) {
        el.style.setProperty('font-size', '17px', 'important');
        el.style.setProperty('line-height', '1.16', 'important');
        el.style.setProperty('margin-bottom', '4px', 'important');
      });

      form.querySelectorAll('p,label').forEach(function (el) {
        el.style.setProperty('font-size', '10.5px', 'important');
        el.style.setProperty('line-height', '1.28', 'important');
      });

      form.querySelectorAll('input:not([type="checkbox"]),select,button').forEach(function (el) {
        el.style.setProperty('height', '36px', 'important');
        el.style.setProperty('min-height', '36px', 'important');
        el.style.setProperty('font-size', '12.5px', 'important');
      });

      form.querySelectorAll('input[type="checkbox"]').forEach(function (el) {
        el.style.setProperty('width', '12px', 'important');
        el.style.setProperty('height', '12px', 'important');
        el.style.setProperty('min-height', '12px', 'important');
      });
    });

    hideSpurCloseButton();
  }

  function spurPopupVisible() {
    return findSpurForms().length > 0;
  }

  function clickSpurTrigger() {
    var triggers = Array.prototype.slice.call(
      document.querySelectorAll('button,a,[role="button"]')
    ).filter(function (el) {
      if (!isSpurElement(el) || !isVisible(el)) return false;

      var t = textOf(el);

      if (t.indexOf('close') > -1 || t.indexOf('dismiss') > -1 || t.trim() === 'x' || t.trim() === '×') return false;
      if (t.indexOf('privacy') > -1 || t.indexOf('terms') > -1) return false;

      return (
        t.indexOf('offer') > -1 ||
        t.indexOf('get') > -1 ||
        t.indexOf('sign') > -1 ||
        t.indexOf('subscribe') > -1 ||
        t.indexOf('discount') > -1 ||
        t.indexOf('unlock') > -1
      );
    });

    if (triggers[0]) triggers[0].click();
  }

  function triggerSpurPopup() {
    if (localStorage.getItem(SUBMITTED_KEY) === 'yes') return;

    compactSpurFormOnly();

    if (spurPopupVisible()) return;

    clearSpurDismissMemory();

    setTimeout(function () {
      clickSpurTrigger();

      setTimeout(compactSpurFormOnly, 500);
      setTimeout(compactSpurFormOnly, 1200);
      setTimeout(compactSpurFormOnly, 2500);
    }, 300);
  }

  document.addEventListener('submit', function (event) {
    if (isSpurElement(event.target)) {
      localStorage.setItem(SUBMITTED_KEY, 'yes');
    }
  }, true);

  setTimeout(triggerSpurPopup, FIRST_DELAY);
  setInterval(triggerSpurPopup, REPEAT_DELAY);

  new MutationObserver(compactSpurFormOnly).observe(document.body, {
    childList: true,
    subtree: true
  });
})();
</script>
{% endunless %}
<style>
  .tvastra-size-empty .form-label span {
    color: #8b8179 !important;
    font-weight: 400 !important;
  }

  .main-product-page .product-variants fieldset.size-filter.tvastra-size-empty .variant-option label,
  .main-product-page .product-variants fieldset.size-filter.tvastra-size-empty .variant-option input[type="radio"]:checked + label,
  .product-variants fieldset.size-filter.tvastra-size-empty .variant-option.selected label {
    background: #fff !important;
    color: #111 !important;
    border-color: #d8d1ca !important;
    box-shadow: none !important;
    transform: none !important;
  }

  .product-variants fieldset.size-filter.tvastra-size-empty .variant-option label .text-swatch,
  .product-variants fieldset.size-filter.tvastra-size-empty .variant-option input[type="radio"]:checked + label .text-swatch,
  .product-variants fieldset.size-filter.tvastra-size-empty .variant-option.selected .text-swatch {
    background: transparent !important;
    color: #111 !important;
    border-color: transparent !important;
  }

  .tvastra-size-message {
    display: none;
    margin-top: 10px;
    padding: 10px 12px;
    border: 1px solid #eadfd5;
    border-radius: 8px;
    background: #fffaf5;
    color: #2b2520;
    font-size: 12px;
    line-height: 1.35;
    width: 100%;
    max-width: min(310px, 100%);
    box-sizing: border-box;
    white-space: normal !important;
    overflow-wrap: anywhere;
    word-break: normal;
    box-shadow: 0 10px 24px rgba(0,0,0,.08);
  }

  .tvastra-size-message.show {
    display: block;
  }

  .tvastra-size-error .tvastra-size-message {
    border-color: #111;
  }
</style>

<script>
(function () {
  if (window.tvastraSizeProfessionalV5) return;
  window.tvastraSizeProfessionalV5 = true;

  function closest(el, selector) {
    return el && el.closest ? el.closest(selector) : null;
  }

  function getArea(el) {
    return closest(el, '.product-info, .product-info-right') ||
      document.querySelector('.product-info, .product-info-right');
  }

  function getSizeBox(area) {
    var box = area && area.querySelector('.product-variants fieldset.size-filter');
    if (!box || box.offsetParent === null) return null;
    return box.querySelector('input[type="radio"], select') ? box : null;
  }

  function getMessage(box) {
    var msg = box.querySelector('.tvastra-size-message');
    if (msg) return msg;

    msg = document.createElement('div');
    msg.className = 'tvastra-size-message';
    msg.textContent = 'Please select your size before continuing.';
    box.appendChild(msg);
    return msg;
  }

  function clearVisualSize(box) {
    if (!box || box.dataset.sizeChosenByUser === 'true') return;

    box.classList.add('tvastra-size-empty');

    var labelValue = box.querySelector('.form-label span');
    if (labelValue) labelValue.textContent = 'Select your size';

    box.querySelectorAll('input[type="radio"]').forEach(function (input) {
      input.checked = false;
      input.removeAttribute('checked');
    });

    box.querySelectorAll('.variant-option.selected, .swatches-hover.selected').forEach(function (item) {
      item.classList.remove('selected');
      item.removeAttribute('data-selected');
    });
  }

  function markChosen(box, value) {
    box.dataset.sizeChosenByUser = 'true';
    box.classList.remove('tvastra-size-empty', 'tvastra-size-error');

    var labelValue = box.querySelector('.form-label span');
    if (labelValue && value) labelValue.textContent = value;

    getMessage(box).classList.remove('show');
  }

  function initBox(box) {
    if (!box) return;

    if (box.dataset.tvastraSizeInit !== 'true') {
      box.dataset.tvastraSizeInit = 'true';
      box.dataset.sizeChosenByUser = 'false';

      box.addEventListener('click', function (event) {
        var option = closest(event.target, '.variant-option');
        if (!option) return;

        var input = option.querySelector('input[type="radio"]');
        markChosen(box, input ? input.value : '');
      }, true);

      box.addEventListener('change', function (event) {
        if (event.target.matches('input[type="radio"], select')) {
          markChosen(box, event.target.value);
        }
      }, true);
    }

    clearVisualSize(box);
  }

  function initAll() {
    document.querySelectorAll('.product-variants fieldset.size-filter').forEach(initBox);
  }

  function blockIfSizeMissing(target, event) {
    var box = getSizeBox(getArea(target));
    if (!box) return false;

    initBox(box);

    if (box.dataset.sizeChosenByUser === 'true') return false;

    event.preventDefault();
    event.stopPropagation();
    event.stopImmediatePropagation();

    clearVisualSize(box);
    box.classList.add('tvastra-size-error');
    getMessage(box).classList.add('show');
    box.scrollIntoView({ behavior: 'smooth', block: 'center' });

    return true;
  }

  function openStockNotification(event) {
    var popup = document.querySelector('.back-in-stock-popup');
    if (!popup || !window.jQuery || !window.jQuery.magnificPopup) return false;

    event.preventDefault();
    event.stopPropagation();
    event.stopImmediatePropagation();

    var selectedOptions = document.querySelectorAll('.main-product-page .product-variants input:checked');
    var selectedVariant = Array.prototype.map.call(selectedOptions, function (option) {
      return option.value;
    }).filter(Boolean).join(' / ');
    var variantField = popup.querySelector('[name="contact[variant]"]');
    if (variantField) variantField.value = selectedVariant;

    window.jQuery.magnificPopup.open({
      items: {
        src: popup.innerHTML,
        type: 'inline'
      }
    });
    return true;
  }

  document.addEventListener('click', function (event) {
    var btn = closest(event.target, '[name="add"], .cart-btn, .product-buy-now-inline, .shopify-payment-button, .shopify-payment-button__button, custom-btn, [data-testid="Checkout-button"]');
    if (!btn) return;
    if (btn.classList.contains('notify-me')) {
      openStockNotification(event);
      return;
    }
    blockIfSizeMissing(btn, event);
  }, true);

  document.addEventListener('submit', function (event) {
    var form = closest(event.target, 'form[data-type="add-to-cart-form"], form[action*="/cart/add"]');
    if (form) blockIfSizeMissing(form, event);
  }, true);

  initAll();
  setTimeout(initAll, 300);
  setTimeout(initAll, 1000);
  window.addEventListener('load', initAll);
})();
</script>


  </body>
</html>
